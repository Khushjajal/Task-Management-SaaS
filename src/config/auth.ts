import type { User } from "@/types";

export const TEMP_AUTH_DISABLED = true;

export const TEMP_USER: User = {
  id: "temporary-user",
  _id: "temporary-user",
  username: "Guest User",
  email: "guest@collabflow.local",
  avatar: "",
};
